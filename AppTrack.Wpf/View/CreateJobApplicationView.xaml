<Window x:Class="AppTrack.WpfUi.View.CreateJobApplicationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create Job Application" Height="300" Width="400">
    <StackPanel >
        <Grid Margin="10,10,10,10">
            <Grid.RowDefinitions>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Label HorizontalAlignment="Right" Grid.Row="0" Grid.Column="0">Client name:</Label>
            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding JobApplication.ClientName}"/>
            <Label HorizontalAlignment="Right" Grid.Row="1" Grid.Column="0">Position:</Label>
            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding JobApplication.Position}"/>
            <Label HorizontalAlignment="Right" Grid.Row="2" Grid.Column="0">Url:</Label>
            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding JobApplication.URL}"/>
            <Label HorizontalAlignment="Right" Grid.Row="3" Grid.Column="0">Application Text:</Label>
            <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding JobApplication.ApplicationText}"/>
        </Grid>
        <StackPanel Margin="10" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Save" Command="{Binding SaveCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" HorizontalAlignment="Center" Margin="5"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" HorizontalAlignment="Center" Margin="5"/>
        </StackPanel>
    </StackPanel>
</Window>
